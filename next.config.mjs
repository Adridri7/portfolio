/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['avatars.githubusercontent.com', 'images.pexels.com', 'cdn.discordapp.com'],
  },
};

export default nextConfig;
